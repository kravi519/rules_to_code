PyYAML>=6.0

# Real-event stack (commented out here — this sandbox has no network to
# install them, and dataclasses/heuristics stand in for them; see README.md):
# pydantic>=2.0
# pytest>=7.0
streamlit>=1.30
